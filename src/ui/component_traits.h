/* UI component traits — crates/ui/src/component_traits.rs.

   The UI façade re-exports Base's POD state flags; C++ expresses the Rust
   traits as fluent methods on each component, with this shared state shape. */

#include "base/component_traits.h"
